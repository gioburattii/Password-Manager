name: password_manager
description: A secure password manager app for mobile and web.

version: 1.0.0+1

environment:
  sdk: ">=3.1.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  firebase_core: ^3.15.1
  firebase_auth: ^5.6.2
  google_sign_in: ^6.1.6
  cloud_firestore: ^5.6.11
  shared_preferences: ^2.2.2
  uuid: ^4.0.0



dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true

